_target_: dataset.inpaint.InpaintDataModule
name: ahn4

height: 512
width: 512

num_workers: 4
batch_size: 16

normaliser:
  gain: 10.0
  scale: 0.01
  nodata: 0.0

data:
  train_dir: ${data_dir}/ahn4/images/data_large
  val_dir: ${data_dir}/ahn4/images/val_large
  test_dir: ${data_dir}/ahn4/images/test_large
  tocomplete_dir: ${data_dir}/ahn4/tocomplete_ams/images
  checkpoint: ${data_dir}/ahn4/weights/best.ckpt

mask:
  train_dir: ${data_dir}/ahn4/masks
  val_dir: ${data_dir}/ahn4/masks
  test_dir: ${data_dir}/ahn4/masks
  tocomplete_dir: ${data_dir}/ahn4/tocomplete_ams/masks
  threshold: 0.9

